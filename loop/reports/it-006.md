# it-006 — T12 single-earnings-row anchor — ACCEPTED
Change: on OCR-injected pages, a single-current-period-row column may anchor. A
non-degenerate exact row product whose amount the page REPRINTS as another cents-form
token, x-aligned on a different line — the column's own printed total — forms a
one-member anchored run; everything downstream (S3, V1–V3, the band search,
certainty "low") is the committed chain, untouched. Flag: REALDOOR_OCR_SINGLE_ROW
(0 = byte-identical output).

## Attribution
| corpus | inspected while designing? | correct | wrong | abstain |
|---|---|---|---|---|
| pack     | authored by organizer | 159/159 | 0 | 0 |
| external | inspected_dev         | 23/44   | 0 | 21 |
| confirm  | inspected_dev         | 26/126  | 0 | 100 |

The gain is on confirm, a development set read field-by-field during design. Weaker
evidence than a hold-out; labeled so here and in the ledger.

## Flips
Predicted 1, observed 1, identical:
lcc gross_pay (325.81) — image_only -> correct, closed on page 3's crops:
31.00 x $10.510000 = $325.81 exact, `Total: $325.81` x-aligned beneath.

## The finding that shrank the target (why +1, not the design's best-case 2)
The dossier OCR'd lcc at render scale 3.0; the injection layer reads at native
96–116 dpi, where the evidence FRAGMENTS across pages (measured,
loop/worktmp/ocr_design/probe_t12.py): p1 — where the 61.36 + 264.45 = 325.81 band
lives — loses the hours token `31.00` and the summary's current gross, so no anchor
can form there; p3 reads the anchor whole but its crops carry no band member. No
page co-locates anchor and band, so net_pay is honestly unreachable per-page and
stays abstained — recorded on T12 at close. Cross-page evidence joining was weighed
and refused: an identity is licensed by ONE page's own printing, and joining
independently-misreadable renderings multiplies exactly the correlated-misread
surface the license exists to refuse.

## Falsification
Fired on 1 of 77 documents (the target only), zero conflicts.
loop/falsification/it-006.json.

## The license: printed total closed by an arithmetic identity
Two independent printings must agree to the cent AND a printed product must close
over one of them. The document's own misread family is the hazard, measured live,
and every refusal held: `'5113.20'` (a `$` read as a digit — parses as money; no
product closes over it, nothing reprints it), `'$4,209.35'` on p3 vs `'$4,209.38'`
on p1 (the reprint-equality refusal in the flesh), `'Sap 15, 2017'` and Banner ID
`'X00123458'` (unemittable — VERIFIABLE_FIELDS is the numeric four). All pinned as
must-stay-abstained tests; lcc's expect_absent hourly_rate/regular_hours are refused
structurally (one row can never resolve the factor columns) and pinned. A
same-baseline twin (osu's Current = YTD) is not a reprint — pinned.

## Gates: G1–G7 all pass. pytest 1397 -> 1417. Pack 159/159, IoU mean 0.967727,
byte-identical with the flag off (G5 vs 98a1b65).

## Cost: 1 expensive call. ## Anomalies: none.

## Loop state after this iteration
The OCR plan (design it-A..it-D, backlog T9..T12) is fully walked: 9 of the 22
loop-visible image_only fields now read correct, zero wrong anywhere, and the other
13 are classified (12 NO_SAFE_RULE at T13, 1 measured-fragmentation abstention on
T12). T2/T3/T4 (external text-path items) remain OPEN for a future run; STOP.md is
theirs to write.
