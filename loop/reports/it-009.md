# it-009 — T16 — the vocabulary reads the apostrophe the page actually prints — ACCEPTED (operator-closed)
Change: `normalize_label` folds the frozen scorer's four quote glyphs (`‘ ’ → '`,
`“ ” → "` — eval/score_extraction.py `_QUOTE_MAP` lines 156–159, quote rows only,
no dashes) before matching, so wa_dshs's printed `EMPLOYEE’S NAME` (U+2019)
matches the ASCII `EMPLOYEE'S NAME` already in `LABEL_SYNONYMS`. With it ships
the guard its own falsification demanded: `_parallel_caption_refusal` — a
candidate that is caption-shaped AND repeats the label's own final token is the
page printing the next column's caption, refused at `_side_by_side_run` and
`columns.column_value`. Flag: REALDOOR_QUOTE_FOLD (default on; `=0` restores).

## The result this iteration exists for
Zero flips, and that is the true sentence, not a shortfall: the wa_dshs filled
values live in AcroForm widgets the text path cannot see (verified in the
annots), so recognising the caption moves no field today. The yield is that the
label vocabulary now matches what real forms print — and the diagnosis note's
"may move ZERO fields" was re-verified and CORRECTED: the naive fold alone
manufactures a WRONG (`person_name = "EMPLOYER’S NAME"` on wa_dshs blank —
expect_absent — and its filled twin, truth 'Terrence Boyd'), measured before any
code, refused by the shipped guard. Corpus census (the enumeration the fold is
held to): U+2019 ×67, U+201C ×31, U+201D ×31, U+2018 ×0, no width variants.
filled dev: 12.5% / 0 WRONG before, 12.5% / 0 WRONG after, per-doc identical.

## Gate record
Substantive gates clean: pack 159/159 IoU 0.9677, external 23/44, confirm
26/126, all wrong 0; pytest 1539/0 (baseline 1491); G5 flag-off byte-identical;
flips over the 77 manifest: none predicted, none observed. Falsification first:
fired 1/77 (wa_dshs blank, engagement-only) + 1/3 filled dev (its twin), zero
conflicts, zero emission diffs anywhere (loop/falsification/it-009.json).
G6: failed on `testdata/blank_carriers_raw/*` (5 paths) only — a concurrent
workstream's corpus drop landing mid-iteration, after this iteration's dirt
refresh had already registered that workstream's api/logic/ui edits; every
outside-allowlist path is theirs, none touches core/, tests, loop/ or a
protected instrument (the it-007/it-008 documented convention).
G7: failed by REGISTRY SCOPE, not by substance — T16's field list is empty by
design and the predicted flip set is empty, so "observed ∩ target ≠ ∅" is
structurally unsatisfiable; the no-flip claim is evidenced by
scripts/measure_filled_forms.py (identical before/after) and the zero-diff
falsification sweep. Same gate-instrument gap it-008 filed: field_state does
not enumerate the filled corpus.

## The hazard, named
Over-folding is held off by enumeration: the fold set is pinned test-for-test
against the scorer's own `_QUOTE_MAP` (quote rows fold, dash rows measurably do
NOT — `TAKE-HOME PAY` keeps distinguishing its hyphen, orangeusd's U+2010 ×14
untouched). The guard is monotone (reading → abstention only) and its
over-refusal shape — a genuine value ending in its label's head token — has
zero instances in 77 + filled dev; a genuine name in the same geometry still
emits (pinned). The parallel-caption misread was latent for ASCII twins too;
the guard covers those the day a form prints them.

## Successors: none filed. T17 (colon-line geometry) is next per the backlog.
## Cost: 1 expensive call.
