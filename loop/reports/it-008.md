# it-008 — T15 — the corpus's first measured WRONG value is closed by a period license — ACCEPTED (operator-closed)
Change: `RATE OF PAY` binds to hourly_rate only when the page settles the period —
an hourly marker printed on the label's own line, or the rate closing
rate × hours = gross with same-page figures. Unlicensed bindings abstain with the
refusal note. Flag: REALDOOR_RATE_PERIOD_LICENSE (default on; =0 restores the
old emission).

## The result this iteration exists for
filled dev set: 12.5% / **1 WRONG → 12.5% / 0 WRONG** (orangeusd hourly_rate
4,812.00 — a monthly certificated salary — now abstains; truth lists the field
expect_absent, so abstention is the correct terminal). up_016's genuine hourly
rate keeps its value byte-identically: the closure license passes it.

## Gate record
Substantive gates clean: pack 159/159 IoU 0.9677, external 23/44, confirm 26/126,
all wrong 0; pytest 1490; flips over the 77-manifest: none predicted, none observed.
G6: contaminated by the two-page consolidation's commits landing mid-iteration —
every outside-allowlist path belonged to that workstream (it-007's documented
convention). G7: failed by REGISTRY SCOPE, not by substance — the target field
lives in the filled corpus, which the gate's field_state does not enumerate, so
"target fields flipped" is structurally empty there; the flip is evidenced by
scripts/measure_filled_forms.py output quoted above. Filed as a gate-instrument
gap: extending field_state to the filled corpus is loop-tooling work for a future
iteration.

## The hazard, named
The anchoring twin HOURS/UNITS (working days read as hours) is pinned as a
must-stay-out test. The seattle "(circle one) hourly weekly monthly" menu was
verified: the text layer carries all three words equally, so menus license
nothing.

## Successors filed: T16 curly-quote fold, T17 colon-line geometry.
## Cost: 1 expensive call.
