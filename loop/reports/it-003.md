# it-003 — T9 OCR word injection — ACCEPTED
Change: embedded-image regions are rendered and recognised, the detections become the
same Word structures the text path uses, and every existing rule — labels, columns,
two-axis, the arithmetic identity chain, caption refusal — does all accepting and
refusing. Flag: REALDOOR_OCR_WORDS (0 = module never imported, extraction byte-identical).

## Attribution
| corpus | inspected while designing? | correct | wrong | abstain |
|---|---|---|---|---|
| pack     | authored by organizer | 159/159 | 0 | 0 |
| external | inspected_dev         | 23/44   | 0 | 21 |
| confirm  | inspected_dev         | 21/126  | 0 | 105 |

The gains are on confirm, a development set read field-by-field during design. Weaker
evidence than a hold-out; labeled so here and in the ledger.

## Flips
Predicted 4, observed 4, identical:
ou gross_pay (1251.09), ou net_pay (750.14), ou hourly_rate (12.4808),
osu gross_pay (5994.0) — all image_only -> correct.
ou gross/net exceed the design's +2 floor: its scale-3.0 simulation refused them; the
real pipeline renders the region at native scale and the identity chain closes.

## Falsification
Fired on 2 of 77 documents, zero conflicts. 24 of 77 receive injected words; the
identity chain accepts values on exactly 2.

## The license: printed total closed by an arithmetic identity
An OCR digit string is admitted only when the page's own arithmetic closes over it —
the shredded.py discipline applied to pixels. Values the chain cannot close (all dates,
all names, the hi_ags multiplicity page, the up_014 567-vs-700 adjacency trap) stay
abstained. Certainty capped at "low"; every accepted value's note names its identity.

## The hazard, named
A digit misread surviving the identity by coincidence. Refused by requiring the full
chain, not one equation; the dossier's measured traps (567.00 nearest to GROSSPAY with
the true 700.00 unreadable; Banner ID digit flip at 96-116 dpi) are pinned as tests
that must stay abstained.

## Gates: G1–G7 all pass. pytest 1276 -> 1331. Pack byte-identical with the flag off.
## Cost: 1 expensive call. ## Anomalies: none.

## Operator note
The implementing agent stalled awaiting its own gate run (third R25 occurrence); the
operator ran the gate with the proposal's declared invocation and performed P6
bookkeeping. Gate PASS on first properly-invoked run.
