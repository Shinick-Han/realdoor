# it-010 — T17 — the label's own colon licenses the fill-in on its line — ACCEPTED (operator-closed)
Change: a colon-terminated recognised label may read the immediately following
run on its own baseline at a word-space gap (a bare label keeps the 12pt column
gap), and for that read its own line is a fill-in line, not a column-header row
(the own-line amnesty), with the interior-colon refusal holding the licensed
read to a clean fill or nothing. One seam: `_side_by_side_run` /
`_side_by_side_value`. Flag: REALDOOR_COLON_GAP (default on; `=0` restores).

## The result this iteration exists for
filled dev set: **12.5% (3/24) → 16.7% (4/24), 0 WRONG throughout** — seattle
`person_name` reads `Marisol Vega` (truth, by construction) through `Employee
Name:`'s own printed colon. Predicted 1 flip, observed 1 flip, and the
77-manifest moved nothing.

## The diagnosis, corrected on the page
The backlog blamed the 5.0pt gap under SIDE_BY_SIDE_MIN_GAP. Measured, the page
refuses TWICE: the filled line `Employee Name: Marisol Vega Job Title: Prep
Cook` is four letters-only runs, so `_header_row_words` classifies the fill-in
line as a column-header row — the value becomes its own barrier and a refused
"header cell" — and only behind that sits the gap floor. The license answers
both, and no more: the colon is the page punctuating "what follows me on my
line is my value" (the same print `_caption_refusal` already trusts in the
opposite direction), and a column-header cell — naming the column BELOW it —
does not print one. Everything else stands, pinned by test: sole-run cell,
colon barriers (the empty blank-form cell reads nothing), colon candidates
never values, other lines' header rows, the it-009 parallel-caption refusal,
parse gates.

## Gate record
G1–G6 ALL PASS: pack 159/159 IoU 0.9677, external 23/44, confirm 26/126, all
wrong 0; pytest 1588/0 (baseline 1540); G5 flag-off byte-identical to b173e7b;
G6 clean — the scenario workstream's files committed mid-iteration (b92e5ff,
cf1b961) and this iteration's diff is exactly its three-file allowlist.
G7: failed by REGISTRY SCOPE only — the target field lives in the filled
corpus, which field_state does not enumerate, so "observed ∩ target ≠ ∅" is
structurally unsatisfiable even with a real flip; evidenced instead by
scripts/measure_filled_forms.py (4/24, seattle correct 1, wrong 0) and
loop/falsification/it-010.json. Same instrument gap it-008/it-009 filed.
Falsification first: fired 0/77 (the license engages nowhere on the manifest —
strong specificity), filled dev fired on the target only, zero conflicts.
One G4 round-trip, the it-005 precedent: it-008's pin asserting the whole
seattle fill abstains contradicted the intended flip; the pin was UPDATED
(held under REALDOOR_COLON_GAP=0), never removed, and a sibling pin now holds
the rate abstention under the license. The interior-colon refusal was added at
P4 after this iteration's own hand-built-geometry test found the amnesty could
hand back a merged caption pair (`Job Title: Prep Cook` as one run on a
header-classified line); the predicate was amended to carry it and the sweep
re-run — refusal-biased, zero behaviour change anywhere measured.

## The hazard, named
Prose after a colon self-refuses in layers (word-spaced prose is one run and
matches no key; non-vocabulary captions license nothing; typed fields must
parse), and the colon-dense instructional confirm documents moved nothing in
the sweep. The free-text residual — a vocabulary colon label, a run-splitting
gap, a sole letters-only tail that is not the value — has zero instances in
77 + filled dev; the record is the sweep.

## Bookkeeping note
T19–T21 (scenario corpus findings) were filed into backlog.json by the
concurrent workstream mid-iteration and ride along in this commit untouched.

## Successors: none filed. T2/T3/T4 remain the open manifest targets.
## Cost: 1 expensive call.
